# Secrets : clés API démo/réel Bitget — JAMAIS committées (voir credentials.local.json)
credentials*
!credentials*.example.json
API_code.txt

# Journaux de décisions (NDJSON, régénérables) — comme le pilier indices
journaux/
