[pytest]
# Les tests importent les paquets top-level `backend` et `gui` -> racine du repo
# sur le sys.path. testpaths borne la collecte au dossier tests/.
pythonpath = .
testpaths = tests
addopts = -q
