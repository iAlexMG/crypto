name: crypto-agg
channels:
  - conda-forge
dependencies:
  - python=3.12
  - pip
  - pip:
      # Bornes HAUTES epinglees au major suivant (build reproductible).
      - websockets>=12.0,<17
      - numpy>=1.26,<3
      - pyqtgraph>=0.13,<0.15
      - PySide6>=6.6,<7
      - tzdata>=2024.1
      # --- tests (dev) : python -m pytest ---
      - pytest>=8.0,<10
